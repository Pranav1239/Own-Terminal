package ssh

// "fmt"
// "os/exec"

// SigninToSSH signs in to an SSH server with the provided host and user.
// func SigninToSSH(host, user string) error {
// cmd := exec.Command("ssh", fmt.Sprintf("%s@%s", user, host))
// cmd.Stdout = os.Stdout
// cmd.Stdin = os.Stdin
// cmd.Stderr = os.Stderr

// err := cmd.Run()
// if err != nil {
// 	return fmt.Errorf("error signing in to SSH: %v", err)
// }

// return nil
// }
